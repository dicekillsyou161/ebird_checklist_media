# Bot token: https://discord.com/developers/applications → your app → Bot → Reset Token
DISCORD_TOKEN=

# Optional: a server (guild) ID for instant slash-command registration while testing.
# Without it, commands register globally, which can take up to an hour to appear.
# GUILD_ID=
